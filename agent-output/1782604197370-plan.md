# Agent output

Mission:

Build a useful feature and open a PR.

## Model response

{
  "summary": "Implement credential and form state persistence in localStorage with remember-me controls, add sample templates dropdown, provide clear button, and generate live links to workflow logs",
  "files": [
    {
      "path": "apps/web/src/main.tsx",
      "content": "import React, { useState, useEffect } from 'react';\nimport { createRoot } from 'react-dom/client';\nimport './style.css';\n\ntype FormState = {\n  token: string;\n  modelAccess: string;\n  repo: string;\n  branch: string;\n  model: string;\n  jobId: string;\n  maxSteps: string;\n  mission: string;\n};\n\nconst DEFAULT_STATE: FormState = {\n  token: '',\n  modelAccess: '',\n  repo: 'gkjuwon-tech/PRBOT',\n  branch: 'main',\n  model: 'gemini-3.5-flash',\n  jobId: '',\n  maxSteps: '3',\n  mission: 'Build a useful feature and open a PR.'\n};\n\nconst SAMPLE_MISSIONS = [\n  \"Build a useful feature and open a PR.\",\n  \"Add a new REST API endpoint to apps/api/src/server.ts to list jobs filtered by repoFullName.\",\n  \"Improve apps/web/src/style.css with helper utility classes and dark theme variables.\",\n  \"Refactor scripts/cloud-agent.mjs to extract the GitHub and Gemini logic into helper modules in packages/shared.\"\n];\n\nfunction parseRepo(value: string) {\n  const cleaned = value.trim().replace(/^https:\\/\\/github\\.com\\//, '').replace(/\\.git$/, '');\n  const [owner, repo] = cleaned.split('/');\n  if (!owner || !repo) throw new Error('Repo must look like owner/name');\n  return { owner, repo };\n}\n\nfunction makeJobId(value: string) {\n  const slug = value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').slice(0, 40);\n  return slug || `job-${Date.now()}`;\n}\n\nfunction App() {\n  const [form, setForm] = useState<FormState>(() => {\n    try {\n      const saved = localStorage.getItem('hn_pr_agent_form');\n      if (saved) {\n        const parsed = JSON.parse(saved);\n        return { ...DEFAULT_STATE, ...parsed };\n      }\n    } catch (e) {\n      console.error('Failed to load saved form settings:', e);\n    }\n    return DEFAULT_STATE;\n  });\n\n  const [remember, setRemember] = useState(() => {\n    return localStorage.getItem('hn_pr_agent_remember') !== 'false';\n  });\n\n  const [busy, setBusy] = useState(false);\n  const [message, setMessage] = useState<React.ReactNode>('ready. feed me a mission.');\n\n  useEffect(() => {\n    localStorage.setItem('hn_pr_agent_remember', String(remember));\n    if (remember) {\n      localStorage.setItem('hn_pr_agent_form', JSON.stringify(form));\n    } else {\n      const { token, modelAccess, ...publicForm } = form;\n      localStorage.setItem('hn_pr_agent_form', JSON.stringify(publicForm));\n    }\n  }, [form, remember]);\n\n  function handleReset() {\n    if (window.confirm('Are you sure you want to reset all inputs and clear stored credentials?')) {\n      localStorage.removeItem('hn_pr_agent_form');\n      localStorage.removeItem('hn_pr_agent_remember');\n      setForm(DEFAULT_STATE);\n      setRemember(true);\n      setMessage('Settings reset to defaults.');\n    }\n  }\n\n  async function submit(event: React.FormEvent) {\n    event.preventDefault();\n    setBusy(true);\n    setMessage('calling GitHub Actions... little orange goblin is putting on boots.');\n    try {\n      const { owner, repo } = parseRepo(form.repo);\n      const jobId = form.jobId.trim() || makeJobId(form.mission);\n      const res = await fetch(`https://api.github.com/repos/${owner}/${repo}/actions/workflows/cloud-agent-phone.yml/dispatches`, {\n        method: 'POST',\n        headers: {\n          accept: 'application/vnd.github+json',\n          authorization: `Bearer ${form.token}`,\n          'x-github-api-version': '2022-11-28',\n          'content-type': 'application/json'\n        },\n        body: JSON.stringify({\n          ref: form.branch,\n          inputs: {\n            mission: form.mission,\n            base_branch: form.branch,\n            model: form.model,\n            model_access: form.modelAccess,\n            job_id: jobId,\n            max_steps: form.maxSteps\n          }\n        })\n      });\n      if (!res.ok) throw new Error(`${res.status} ${await res.text()}`);\n      \n      const workflowUrl = `https://github.com/${owner}/${repo}/actions/workflows/cloud-agent-phone.yml`;\n      setMessage(\n        <span>\n          workflow started for <b>{jobId}</b>. Use the same Job ID to continue. Open{' '}\n          <a href={workflowUrl} target=\"_blank\" rel=\"noopener noreferrer\" style={{ color: '#ff6600', textDecoration: 'underline', fontWeight: 'bold' }}>\n            GitHub Actions Workflow Logs\n          </a>\n        </span>\n      );\n    } catch (error) {\n      setMessage(error instanceof Error ? error.message : String(error));\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  return (\n    <main>\n      <header><b>HN Cloud PR Agent</b><span>new | run | continue | memory | prs</span></header>\n      <section className=\"layout\">\n        <aside>\n          <h1>Run from phone</h1>\n          <p>This is the app. No laptop. Use the same Job ID to continue a long repo task across multiple runs.</p>\n          <form onSubmit={submit}>\n            <label>GitHub token<input type=\"password\" value={form.token} onChange={e => setForm({ ...form, token: e.target.value })} placeholder=\"fine-grained token with Actions: write\" /></label>\n            <label>Gemini API key<input type=\"password\" value={form.modelAccess} onChange={e => setForm({ ...form, modelAccess: e.target.value })} placeholder=\"paste Gemini API key\" /></label>\n            \n            <label style={{ display: 'flex', alignItems: 'center', gap: '6px', margin: '12px 0', cursor: 'pointer' }}>\n              <input \n                type=\"checkbox\" \n                checked={remember} \n                onChange={e => setRemember(e.target.checked)} \n                style={{ width: 'auto', margin: 0 }} \n              />\n              <span style={{ fontSize: '11px', color: '#555' }}>Remember credentials in this browser</span>\n            </label>\n\n            <label>Repo<input value={form.repo} onChange={e => setForm({ ...form, repo: e.target.value })} /></label>\n            <label>Base branch<input value={form.branch} onChange={e => setForm({ ...form, branch: e.target.value })} /></label>\n            <label>Job ID<input value={form.jobId} onChange={e => setForm({ ...form, jobId: e.target.value })} placeholder=\"same id = continue same task\" /></label>\n            <label>Steps this run<input value={form.maxSteps} onChange={e => setForm({ ...form, maxSteps: e.target.value })} placeholder=\"3\" /></label>\n            <label>Gemini model<select value={form.model} onChange={e => setForm({ ...form, model: e.target.value })}><option>gemini-3.5-flash</option><option>gemini-3.1-pro</option></select></label>\n            \n            <label>\n              Mission Templates\n              <select \n                onChange={e => {\n                  if (e.target.value) {\n                    setForm({ ...form, mission: e.target.value });\n                  }\n                }}\n                defaultValue=\"\"\n                style={{ marginTop: '3px' }}\n              >\n                <option value=\"\" disabled>-- Select template --</option>\n                {SAMPLE_MISSIONS.map((m, i) => (\n                  <option key={i} value={m}>{m.slice(0, 50)}...</option>\n                ))}\n              </select>\n            </label>\n\n            <label>Mission<textarea value={form.mission} onChange={e => setForm({ ...form, mission: e.target.value })} /></label>\n            \n            <div style={{ display: 'flex', gap: '8px', marginTop: '14px' }}>\n              <button disabled={busy || !form.token || !form.modelAccess || !form.mission} style={{ flex: 1, fontWeight: 'bold' }}>{busy ? 'summoning...' : 'run / continue agent'}</button>\n              <button type=\"button\" onClick={handleReset} style={{ background: '#eee', border: '1px solid #888' }}>Reset</button>\n            </div>\n          </form>\n        </aside>\n        <section>\n          <h1>Status</h1>\n          <article className="panel"><p>{message}</p></article>\n          <article className="panel">\n            <h2>Long task mode</h2>\n            <ol>\n              <li>Calls GitHub's workflow dispatch API directly from your browser.</li>\n              <li>Starts <code>cloud-agent-phone.yml</code> in GitHub Actions.</li>\n              <li>The workflow uses Job ID and Steps this run to continue a saved task.</li>\n              <li>The agent records state, logs, tasks, and memory in the repo branch.</li>\n            </ol>\n            <p>The keys go from this page to GitHub's API. There is no separate app backend reading or storing your secrets. Everything runs locally in your browser.</p>\n          </article>\n        </section>\n      </section>\n    </main>\n  );\n}\n\nconst container = document.getElementById('root');\nif (container) {\n  const root = createRoot(container);\n  root.render(<App />);\n}"
    }
  ],
  "taskUpdates": [
    {
      "id": "AW2Zhl5j",
      "status": "done",
      "notes": "Added localStorage persistence, checkbox for remember credentials option, mission templates dropdown selection, quick dynamic link to GitHub Actions run details, and a settings reset button."
    }
  ],
  "newTasks": [
    {
      "title": "Enhance API Server jobs filter",
      "notes": "Add job list filtering by repo name in apps/api/src/server.ts"
    }
  ],
  "memories": [
    {
      "kind": "decision",
      "text": "Saved form inputs (except credentials if remember checkbox is unchecked) to localStorage to make repeating steps convenient on mobile screen sizes."
    }
  ],
  "openQuestions": []
}
