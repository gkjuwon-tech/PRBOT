# Agent output

Mission:

Build a useful feature and open a PR.

## Model response

{
  "summary": "Implement comprehensive Web UI upgrades including safe localStorage credentials persistence toggle, dynamic GitHub action URL mapping, a recent runs log manager, settings reset utility, and detailed usage documentation in README.",
  "files": [
    {
      "path": "apps/web/src/main.tsx",
      "content": "import React, { useState, useEffect } from 'react';\nimport { createRoot } from 'react-dom/client';\nimport './style.css';\n\ntype FormState = {\n  token: string;\n  modelAccess: string;\n  repo: string;\n  branch: string;\n  model: string;\n  jobId: string;\n  maxSteps: string;\n  mission: string;\n};\n\ntype SavedRun = {\n  jobId: string;\n  repo: string;\n  branch: string;\n  mission: string;\n  timestamp: string;\n};\n\nconst DEFAULT_FORM: FormState = {\n  token: '',\n  modelAccess: '',\n  repo: 'gkjuwon-tech/PRBOT',\n  branch: 'main',\n  model: 'gemini-3.5-flash',\n  jobId: '',\n  maxSteps: '3',\n  mission: 'Build a useful feature and open a PR.'\n};\n\nfunction parseRepo(value: string) {\n  const cleaned = value.trim().replace(/^https:\\/\\/github\\.com\\//, '').replace(/\\.git$/, '');\n  const [owner, repo] = cleaned.split('/');\n  if (!owner || !repo) throw new Error('Repo must look like owner/name or a full GitHub repository URL');\n  return { owner, repo };\n}\n\nfunction makeJobId(value: string) {\n  const slug = value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').slice(0, 40);\n  return slug || `job-${Date.now()}`;\n}\n\nexport function App() {\n  const [form, setForm] = useState<FormState>(DEFAULT_FORM);\n  const [saveCreds, setSaveCreds] = useState<boolean>(false);\n  const [recentRuns, setRecentRuns] = useState<SavedRun[]>([]);\n  const [busy, setBusy] = useState(false);\n  const [message, setMessage] = useState('ready. feed me a mission.');\n\n  useEffect(() => {\n    try {\n      const storedSaveCreds = localStorage.getItem('hn_pr_agent_save_creds');\n      const isSavedCreds = storedSaveCreds === 'true';\n      setSaveCreds(isSavedCreds);\n\n      const storedFormStr = localStorage.getItem('hn_pr_agent_form');\n      if (storedFormStr) {\n        const parsed = JSON.parse(storedFormStr);\n        setForm(prev => {\n          const loaded: FormState = { ...prev, ...parsed };\n          if (!isSavedCreds) {\n            loaded.token = '';\n            loaded.modelAccess = '';\n          }\n          return loaded;\n        });\n      }\n\n      const storedRunsStr = localStorage.getItem('hn_pr_agent_runs');\n      if (storedRunsStr) {\n        setRecentRuns(JSON.parse(storedRunsStr));\n      }\n    } catch (e) {\n      console.error('Error loading state from localStorage:', e);\n    }\n  }, []);\n\n  const updateForm = (updates: Partial<FormState>) => {\n    setForm(prev => {\n      const updated = { ...prev, ...updates };\n      try {\n        const toSave = { ...updated };\n        if (!saveCreds) {\n          toSave.token = '';\n          toSave.modelAccess = '';\n        }\n        localStorage.setItem('hn_pr_agent_form', JSON.stringify(toSave));\n      } catch (e) {\n        console.error('Error writing state to localStorage:', e);\n      }\n      return updated;\n    });\n  };\n\n  const handleToggleSaveCreds = (checked: boolean) => {\n    setSaveCreds(checked);\n    try {\n      localStorage.setItem('hn_pr_agent_save_creds', String(checked));\n      if (!checked) {\n        const storedFormStr = localStorage.getItem('hn_pr_agent_form');\n        if (storedFormStr) {\n          const parsed = JSON.parse(storedFormStr);\n          delete parsed.token;\n          delete parsed.modelAccess;\n          localStorage.setItem('hn_pr_agent_form', JSON.stringify(parsed));\n        }\n      } else {\n        localStorage.setItem('hn_pr_agent_form', JSON.stringify(form));\n      }\n    } catch (e) {\n      console.error(e);\n    }\n  };\n\n  const handleReset = () => {\n    if (window.confirm('Are you sure you want to clear all settings, saved credentials, and recent runs history?')) {\n      try {\n        localStorage.clear();\n        setForm(DEFAULT_FORM);\n        setSaveCreds(false);\n        setRecentRuns([]);\n        setMessage('Settings successfully reset to default.');\n      } catch (e) {\n        console.error(e);\n      }\n    }\n  };\n\n  const handleSelectRun = (run: SavedRun) => {\n    updateForm({\n      repo: run.repo,\n      branch: run.branch,\n      mission: run.mission,\n      jobId: run.jobId\n    });\n    setMessage(`Loaded configuration for job ID: ${run.jobId}`);\n  };\n\n  const handleClearHistory = () => {\n    if (window.confirm('Clear recent job history list?')) {\n      localStorage.removeItem('hn_pr_agent_runs');\n      setRecentRuns([]);\n    }\n  };\n\n  async function submit(event: React.FormEvent) {\n    event.preventDefault();\n    setBusy(true);\n    setMessage('calling GitHub Actions... little orange goblin is putting on boots.');\n    try {\n      const { owner, repo } = parseRepo(form.repo);\n      const jobId = form.jobId.trim() || makeJobId(form.mission);\n      \n      const res = await fetch(`https://api.github.com/repos/${owner}/${repo}/actions/workflows/cloud-agent-phone.yml/dispatches`, {\n        method: 'POST',\n        headers: {\n          accept: 'application/vnd.github+json',\n          authorization: `Bearer ${form.token}`,\n          'x-github-api-version': '2022-11-28',\n          'content-type': 'application/json'\n        },\n        body: JSON.stringify({\n          ref: form.branch,\n          inputs: {\n            mission: form.mission,\n            base_branch: form.branch,\n            model: form.model,\n            model_access: form.modelAccess,\n            job_id: jobId,\n            max_steps: form.maxSteps\n          }\n        })\n      });\n\n      if (!res.ok) {\n        const errorText = await res.text();\n        throw new Error(`${res.status} ${errorText || 'Unknown GitHub API error'}`);\n      }\n\n      const newRun: SavedRun = {\n        jobId,\n        repo: form.repo,\n        branch: form.branch,\n        mission: form.mission,\n        timestamp: new Date().toLocaleString()\n      };\n\n      const updatedRuns = [newRun, ...recentRuns.filter(r => r.jobId !== jobId)].slice(0, 15);\n      setRecentRuns(updatedRuns);\n      localStorage.setItem('hn_pr_agent_runs', JSON.stringify(updatedRuns));\n\n      updateForm({ jobId });\n\n      setMessage(`Workflow started successfully for ${jobId}! Use this Job ID to continue or resume the same task. Check action logs here: https://github.com/${owner}/${repo}/actions/workflows/cloud-agent-phone.yml`);\n    } catch (error) {\n      setMessage(error instanceof Error ? error.message : String(error));\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  let workflowUrl = 'https://github.com/gkjuwon-tech/PRBOT/actions/workflows/cloud-agent-phone.yml';\n  try {\n    const { owner, repo } = parseRepo(form.repo);\n    workflowUrl = `https://github.com/${owner}/${repo}/actions/workflows/cloud-agent-phone.yml`;\n  } catch (e) {}\n\n  return (\n    <main>\n      <header>\n        <b>HN Cloud PR Agent</b>\n        <span>new | run | continue | memory | prs</span>\n      </header>\n      <section className=\"layout\">\n        <aside>\n          <h1>Run from phone</h1>\n          <p>This is the app. No laptop. Use the same Job ID to continue a long repo task across multiple runs.</p>\n          <form onSubmit={submit}>\n            <label>\n              GitHub token\n              <input \n                type=\"password\" \n                value={form.token} \n                onChange={e => updateForm({ token: e.target.value })} \n                placeholder=\"fine-grained token with Actions: write\" \n              />\n            </label>\n            <label>\n              Gemini API key\n              <input \n                type=\"password\" \n                value={form.modelAccess} \n                onChange={e => updateForm({ modelAccess: e.target.value })} \n                placeholder=\"paste Gemini API key\" \n              />\n            </label>\n            \n            <div style={{ margin: '8px 0', display: 'flex', alignItems: 'center', gap: '6px' }}>\n              <input \n                type=\"checkbox\" \n                id=\"save_creds_chk\"\n                checked={saveCreds} \n                onChange={e => handleToggleSaveCreds(e.target.checked)} \n                style={{ width: 'auto', margin: 0 }}\n              />\n              <label htmlFor=\"save_creds_chk\" style={{ margin: 0, cursor: 'pointer', fontSize: '11px' }}>\n                Remember credentials in this browser\n              </label>\n            </div>\n\n            <label>\n              Repo\n              <input \n                value={form.repo} \n                onChange={e => updateForm({ repo: e.target.value })} \n                placeholder=\"owner/repo-name\"\n              />\n            </label>\n            <label>\n              Base branch\n              <input \n                value={form.branch} \n                onChange={e => updateForm({ branch: e.target.value })} \n              />\n            </label>\n            <label>\n              Job ID (Optional)\n              <input \n                value={form.jobId} \n                onChange={e => updateForm({ jobId: e.target.value })} \n                placeholder=\"same id = continue same task\" \n              />\n            </label>\n            <label>\n              Steps this run\n              <input \n                value={form.maxSteps} \n                onChange={e => updateForm({ maxSteps: e.target.value })} \n                placeholder=\"3\" \n              />\n            </label>\n            <label>\n              Gemini model\n              <select \n                value={form.model} \n                onChange={e => updateForm({ model: e.target.value })}\n              >\n                <option>gemini-3.5-flash</option>\n                <option>gemini-3.1-pro</option>\n              </select>\n            </label>\n            <label>\n              Mission\n              <textarea \n                value={form.mission} \n                onChange={e => updateForm({ mission: e.target.value })} \n              />\n            </label>\n            <button disabled={busy || !form.token || !form.modelAccess || !form.mission}>\n              {busy ? 'summoning...' : 'run / continue agent'}\n            </button>\n          </form>\n        </aside>\n        \n        <section>\n          <h1>Status</h1>\n          <article className=\"panel\">\n            <p style={{ fontWeight: 'bold', color: '#ff6600', margin: '0 0 8px 0' }}>Agent Signal Panel</p>\n            <p style={{ wordBreak: 'break-all', whiteSpace: 'pre-line', margin: 0 }}>{message}</p>\n          </article>\n\n          {recentRuns.length > 0 && (\n            <article className=\"panel\">\n              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '8px' }}>\n                <h2>Recent Jobs Logged</h2>\n                <button \n                  onClick={handleClearHistory} \n                  style={{ fontSize: '10px', padding: '2px 6px', background: '#f6f6ef' }}\n                >\n                  clear history\n                </button> dress\n              </div>\n              <ul style={{ paddingLeft: '20px', margin: 0, fontSize: '12px', lineHeight: '1.6' }}>\n                {recentRuns.map((run, idx) => (\n                  <li key={run.jobId + '-' + idx}>\n                    <a \n                      href=\"#\" \n                      onClick={(e) => { e.preventDefault(); handleSelectRun(run); }}\n                      style={{ color: '#0055ff', fontWeight: 'bold', textDecoration: 'underline' }}\n                    >\n                      {run.jobId}\n                    </a>{' '}\n                    <small style={{ color: '#666' }}>({run.repo} - {run.timestamp})</small>\n                    <div style={{ color: '#444', fontStyle: 'italic', fontSize: '11px', textOverflow: 'ellipsis', overflow: 'hidden', whiteSpace: 'nowrap', maxWidth: '100%' }}>\n                      Mission: {run.mission}\n                    </div>\n                  </li>\n                ))}\n              </ul>\n            </article>\n          )}\n\n          <article className=\"panel\">\n            <h2>Actions Quick Monitor</h2>\n            <p>\n              View live execution workflow logs and active agent runs directly on GitHub:\n            </p>\n            <p>\n              <a \n                href={workflowUrl} \n                target=\"_blank\" \n                rel=\"noopener noreferrer\" \n                style={{ color: '#ff6600', fontWeight: 'bold', textDecoration: 'underline' }}\n              >\n                Open Workflow on GitHub: {form.repo || 'gkjuwon-tech/PRBOT'} ↗\n              </a>\n            </p>\n          </article>\n\n          <article className=\"panel\">\n            <h2>Long task mode</h2>\n            <ol>\n              <li>Calls GitHub's workflow dispatch API directly from your browser.</li>\n              <li>Starts <code>cloud-agent-phone.yml</code> in GitHub Actions.</li>\n              <li>The workflow uses Job ID and Steps this run to continue a saved task.</li>\n              <li>The agent records state, logs, tasks, and memory in the repo branch.</li>
            </ol>\n            <p>\n              Your credentials are safe: keys go from this browser page directly to GitHub's API. No third-party proxy or database collects your keys.\n            </p>\n            <div style={{ marginTop: '14px', borderTop: '1px solid #ddd', paddingTop: '10px' }}>\n              <button \n                onClick={handleReset} \n                style={{ background: '#fdd', borderColor: '#caa', color: '#822' }}\n              >\n                Reset All Settings & Clear Cached Data\n              </button>\n            </div>\n          </article>\n        </section>\n      </section>\n    </main>\n  );\n}\n\nconst container = document.getElementById('root');\nif (container) {\n  const root = createRoot(container);\n  root.render(\n    <React.StrictMode>\n      <App />\n    </React.StrictMode>\n  );\n}\n"
    },
    {
      "path": "README.md",
      "content": "# HN Cloud PR Agent (PRBOT)\n\nAn intelligent, self-directed repository automation agent designed to continue long-running development tasks and open pull requests directly from your browser, laptop, or mobile phone.\n\n## Features\n\n- **Mobile-first React UI**: Optimized for quick interactions and deployment of complex developer tasks directly from your phone.\n- **Save Credentials Option**: Secure, opt-in local browser storage persistence of GitHub tokens and Gemini API keys so you never have to re-copy-paste credentials on mobile.\n- **Recent Runs Registry**: Browse, retrieve, and auto-resume past jobs using Job IDs.\n- **One-click GitHub Actions Workflows Monitoring**: Seamless deep linking directly to active GitHub workflows with dynamic repository resolving.\n- **Intelligent Long Task Resume**: Automatically records context, memory, saved tasks, and progress to work across multiple action limits without losing work.\n\n## Structure\n\n- `apps/web`: React & Vite application deployed to GitHub Pages.\n- `apps/api`: Fastify backend supporting background tasks with BullMQ & Redis (optional, local-capable).\n- `scripts/cloud-agent.mjs`: The robust core agent orchestrator running directly inside GitHub Actions.\n\n## Getting Started with the Web UI\n\n1. Open the [HN Cloud PR Agent Web App](https://gkjuwon-tech.github.io/PRBOT/).\n2. Enter your fine-grained GitHub Token (needs `Actions: write` and `Contents: write` permissions) and your Gemini API Key.\n3. Choose your repository (e.g. `your-username/your-repo`), base branch, and specify a mission.\n4. Click **Run / Continue Agent**. The web app directly dispatches a workflow to GitHub Actions on your repository!\n"
    }
  ],
  "taskUpdates": [
    {
      "id": "rIkUi-vj",
      "status": "done",
      "notes": "Upgraded the main web application with state management, dynamic workflow monitoring, runs registry, settings reset utility, and detailed usage documentation."
    }
  ],
  "newTasks": [
    {
      "title": "Verify build check on Vite app",
      "notes": "Ensure that the upgraded web app compiles perfectly without type errors."
    }
  ],
  "memories": [
    {
      "kind": "decision",
      "text": "Introduced safe localStorage settings and job history inside the React application to boost mobile-friendly usability."
    }
  ],
  "openQuestions": []
}
